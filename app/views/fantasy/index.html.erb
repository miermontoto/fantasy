<div class="flex flex-col">

  <div class="grid gap-y-2 grid-cols-2 p-2 bg-[#121824] rounded-lg justify-items-stretch">
    <div class="grid gap-y-2 grid-cols-1 p-2">
      <%# For each transfer on @feed_data render a trasfer_card.html.erb %>
      <% @feed_data.each do |transfer| %>
        <%# <%= render 'fantasy/partials/transfer_card', data: transfer %>
        <%= render partial: 'fantasy/partials/transfer_card', locals: { transfer: transfer } %>
      <% end %> 
    </div>

    <div class="">
      <p>Ranking</p>

    </div>
  </div>

</div>